#include "main.h"

/**
 *_abs- función que calcula el valor absoluto de un número ente
 *Return: retorna el valor asboluto si es negativo lo miltiplica por -1
 *@r: variable que cree porque no sabia como hacer
 */

int _abs(int r)
{
	if (r < 0)
		return (-r);
	else
		return (r);


}
